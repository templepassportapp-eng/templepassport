export const API_BASE_URL = process.env.EXPO_PUBLIC_API_BASE_URL ?? 'http://10.0.2.2:8080/api';

// Placeholder identity until Google OAuth is wired in (M1).
// Matches the dev user seeded by backend migration V3.
export const DEV_USER_ID = '00000000-0000-0000-0000-0000000000aa';

export const DEFAULT_RADIUS_M = 750;
